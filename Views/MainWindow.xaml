<Window x:Class="SiapControl.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SiapControl" Height="480" Width="860"
        MinHeight="420" MinWidth="720"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border x:Name="StartupStatusPanel" Grid.Row="0" Padding="12" Margin="0,0,0,12" Background="#EFF6FF" BorderBrush="#BFD7FF" BorderThickness="1" CornerRadius="6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="180"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="StartupStatusText" VerticalAlignment="Center" Text="Preparando base de datos..."/>
                <ProgressBar x:Name="StartupProgressBar" Grid.Column="1" Minimum="0" Maximum="100" Value="0"/>
            </Grid>
        </Border>

        <DataGrid x:Name="UsersGrid" Grid.Row="1" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="Auto" IsReadOnly="True" />
                <DataGridTextColumn Header="Usuario" Binding="{Binding User}" Width="*" />
                <DataGridTextColumn Header="Ruta" Binding="{Binding Path}" Width="2*" />
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button x:Name="CheckUpdateButton" Content="Buscar actualización" Margin="0,0,8,0" IsEnabled="False" />
            <Button x:Name="SearchInstallationsButton" Content="Buscar instalaciones" Margin="0,0,8,0" IsEnabled="False" />
            <Button x:Name="AddUserButton" Content="Agregar" Margin="0,0,8,0" IsEnabled="False" />
            <Button x:Name="UpdateButton" Content="Actualizar" Margin="0,0,8,0" IsEnabled="False" />
            <Button x:Name="ModulesButton" Content="Módulos" IsEnabled="False" />
        </StackPanel>
    </Grid>
</Window>
